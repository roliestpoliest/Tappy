import CoreGraphics

enum Metrics {
    static let windowCornerRadius: CGFloat = 12
    static let titleBarHeight: CGFloat = 52
    static let playbackBarHeight: CGFloat = 60
    static let sidebarWidth: CGFloat = 189
    static let mainWindowSize = CGSize(width: 900, height: 640)
    static let floatingSize = CGSize(width: 126, height: 42)
    static let rowRadius: CGFloat = 10
    static let subtleButtonRadius: CGFloat = 8
}
